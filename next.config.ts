import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    // Add external domains here if you use hosted images later
    // e.g. remotePatterns: [{ protocol: 'https', hostname: 'your-cdn.com' }]
  },
}

export default nextConfig
